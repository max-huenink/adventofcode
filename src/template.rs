use crate::lines::read;

pub fn run_part1() {
    // Read lines of input
    if let Ok(lines) = read("./inputs/dayX/test1.txt") {
        // Iterate over the lines
        for line_result in lines {
            // If the line is okay
            if let Ok(line) = line_result {}
        }
    }
}

pub fn run_part2() {
    // Read lines of input
    if let Ok(lines) = read("./inputs/dayX/test2.txt") {
        // Iterate over the lines
        for line_result in lines {
            // If the line is okay
            if let Ok(line) = line_result {}
        }
    }
}
